@model IEnumerable<AtmRecipeApp.Models.Component>

@{
    ViewData["Title"] = "Bileşen Yönetimi";
}

<div class="container mt-4">
    <div class="row mb-3">
        <div class="col-md-12">
            <h1>🔧 Bileşen Yönetimi</h1>
            <p class="text-muted">ATM bileşenlerini (LCD, CPU vb.) görüntüleyin, düzenleyin veya silin.</p>
            <a asp-action="Create" class="btn btn-success">
                ➕ Yeni Bileşen Ekle
            </a>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0">Bileşen Listesi</h5>
                </div>
                <div class="card-body">
                    @if (Model != null && Model.Any())
                    {
                        <table class="table table-striped table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>ID</th>
                                    <th>Bileşen Adı</th>
                                    <th>Fiyat</th>
                                    <th style="width: 250px;">İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var component in Model)
                                {
                                    <tr>
                                        <td>@component.Id</td>
                                        <td>@component.Name</td>
                                        <td><strong>@component.Price.ToString("C2")</strong></td>
                                        <td>
                                            <a asp-action="Edit" asp-route-id="@component.Id" class="btn btn-sm btn-warning">
                                                ✏️ Düzenle
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@component.Id" class="btn btn-sm btn-danger">
                                                🗑️ Sil
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <div class="alert alert-warning" role="alert">
                            <strong>Uyarı:</strong> Henüz bileşen bulunmamaktadır.
                            <a asp-action="Create" class="alert-link">Buraya tıklayarak</a> yeni bileşen ekleyebilirsiniz.
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>