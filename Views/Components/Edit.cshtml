@model AtmRecipeApp.Models.Component

@{
    ViewData["Title"] = "Bileşen Düzenle";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h1>✏️ Bileşen Düzenle</h1>
            <hr />

            <div class="card">
                <div class="card-body">
                    <form asp-action="Edit" method="post">
                        @Html.AntiForgeryToken()

                        <input type="hidden" asp-for="Id" />

                        <div class="form-group mb-3">
                            <label asp-for="Name" class="form-label">Bileşen Adı</label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Price" class="form-label">Fiyat (₺)</label>
                            <input asp-for="Price" type="number" step="0.01" class="form-control" />
                            <span asp-validation-for="Price" class="text-danger"></span>
                        </div>

                        <div class="alert alert-info" role="alert">
                            <strong>Bilgi:</strong> ID: @Model.Id - Bu bileşeni düzenliyorsunuz.
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-warning">💾 Güncelle</button>
                            <a asp-action="Index" class="btn btn-secondary">❌ İptal</a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="mt-3">
                <a asp-action="Index" class="btn btn-link">← Bileşen Listesine Dön</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}