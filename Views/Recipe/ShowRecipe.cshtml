@model IEnumerable<AtmRecipeApp.Models.ProductComponent>

@{
    ViewData["Title"] = "ATM Reçetesi - " + ViewBag.ProductName;
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <h1 class="mb-4">📋 @ViewBag.ProductName - Reçete</h1>
            <a href="@Url.Action("Index", "Recipe")" class="btn btn-secondary mb-3">
                ← Ürün Listesine Dön
            </a>
        </div>
    </div>

    @if (Model != null && Model.Any())
    {
        <div class="row mt-3">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0">Gerekli Bileşenler</h5>
                    </div>
                    <div class="card-body">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>Bileşen Adı</th>
                                    <th>Miktar</th>
                                    <th>Birim Fiyat</th>
                                    <th>Toplam</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.Component.Name</td>
                                        <td>@item.Quantity adet</td>
                                        <td>@item.Component.Price.ToString("C2")</td>
                                        <td><strong>@((item.Component.Price * item.Quantity).ToString("C2"))</strong></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-12">
                <div class="alert alert-success" role="alert" align="center">
                    <h4 class="alert-heading">💰 Toplam Maliyet</h4>
                    <hr>
                    <p class="mb-0 display-6">@ViewBag.TotalPrice.ToString("C2")</p>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="row mt-3">
            <div class="col-md-12">
                <div class="alert alert-warning" role="alert">
                    <strong>Uyarı:</strong> Bu ürün için henüz bileşen tanımlanmamış.
                </div>
            </div>
        </div>
    }
</div>