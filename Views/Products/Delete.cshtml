@model AtmRecipeApp.Models.Product

@{
    ViewData["Title"] = "Ürün Sil";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h1 class="text-danger">🗑️ Ürün Sil</h1>
            <hr />

            <div class="alert alert-danger" role="alert">
                <h4 class="alert-heading">⚠️ Dikkat!</h4>
                <p>Bu ürünü silmek istediğinize emin misiniz?</p>
            </div>

            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">Silinecek Ürün Bilgileri</h5>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-3">ID:</dt>
                        <dd class="col-sm-9">@Model.Id</dd>

                        <dt class="col-sm-3">Ürün Adı:</dt>
                        <dd class="col-sm-9">@Model.Name</dd>
                    </dl>

                    <form asp-action="Delete" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-danger">
                                🗑️ Evet, Sil
                            </button>
                            <a asp-action="Index" class="btn btn-secondary">
                                ❌ Hayır, İptal Et
                            </a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="mt-3">
                <a asp-action="Index" class="btn btn-link">← Ürün Listesine Dön</a>
            </div>
        </div>
    </div>
</div>